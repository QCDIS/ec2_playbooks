
- add_host:
    hostname: "{{ public_ip }}"
    groups: vms
    ansible_ssh_user: "{{ default_user }}"
    
# - authorized_key:
#     user: "{{ default_user }}"
#     state: present
#     key: 
    
